#include <stdio.h>

int main()
{
        int n;
        char c;

        printf("Digite um caractere e um numero (ex: a 61): ");
        scanf("%c %d", &c, &n);
        printf("%c %d\n", c, n);

        return 0;
}